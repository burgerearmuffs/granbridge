# v0.1.4 UI Polish & Graphics Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax. Work ONLY in the worktree `C:\Users\willa\granbridge-wt-ui` on branch `feat/ui-polish-v0.1.4`. Commit locally; do NOT push, do NOT switch branches, do NOT touch other directories/worktrees (a sibling agent holds the main checkout). Use the Co-Authored-By trailer on commits.

**Goal:** Make GRANBRIDGE feel premium — a 3D dartboard, livelier scoreboards, bigger checkout/win moments, cohesive screens, a no-scroll multiplayer broadcast layout, and a fullscreen toggle — with no new dependencies.

**Architecture:** Additive. Extend existing components with props; add small hooks/CSS; wrap (don't rewrite) the multiplayer screen. Pure SVG/CSS/Web-Audio.

**Tech Stack:** React 18 + TypeScript, Vite, Tailwind, Zustand, Vitest + Testing Library. Tauri v2 (`@tauri-apps/api`). Python bridge unchanged.

**Test command (per slice green-gate, run from worktree root):**
`npm --prefix ui test` then `npm --prefix ui run build`. (Python suites unaffected by UI work, but run `python -m pytest -q` if any `src/` file is touched.)

---

## File structure

- Create `ui/src/useReducedMotion.ts` — hook: respects `prefers-reduced-motion` + the existing video `reducedMotion` setting.
- Create `ui/src/useFullscreen.ts` — hook: Tauri window fullscreen + browser fallback, guarded.
- Create `ui/src/components/FullscreenToggle.tsx` — header button.
- Create `ui/src/components/MpGameLayout.tsx` — multiplayer broadcast-rail shell.
- Modify `ui/src/components/Dartboard.tsx` — 3D treatment, `tilt` + `dart` props.
- Modify `ui/src/views/LiveGame.tsx` — pass play tilt + dart marker.
- Modify `ui/src/components/boards/*.tsx` — score-change/active-player animation (shared patterns).
- Modify `ui/src/index.css` — new keyframes/classes (3D wrapper, tilt, score-roll, win swing).
- Modify `ui/src/views/Multiplayer.tsx` — use `MpGameLayout` for in-progress remote games.
- Modify `ui/src/App.tsx` — mount `FullscreenToggle`; trigger board hero-tilt on win/checkout.
- Modify `ui/src/sound/decide.ts` + `manifest.ts` if cues are missing (verify first).
- Modify the four screen views for the cohesion pass.

---

## Slice 1 — 3D dartboard + gameplay feel

### Task 1.1: Dartboard `tilt` + `dart` props (structure only, flat default preserved)

**Files:** Modify `ui/src/components/Dartboard.tsx`; Test `ui/src/components/Dartboard.test.tsx`.

- [ ] **Step 1 — failing tests.** Add to `Dartboard.test.tsx`:
```tsx
it("defaults to flat (no perspective wrapper) so History is unaffected", () => {
  const { container } = render(<Dartboard />);
  expect(container.querySelector(".dartboard-3d")).toBeNull();
});
it("applies the play-tilt wrapper class when tilt='play'", () => {
  const { container } = render(<Dartboard tilt="play" />);
  expect(container.querySelector(".dartboard-3d.tilt-play")).not.toBeNull();
});
it("renders a dart-landing marker when a dart bed is given", () => {
  const { container } = render(<Dartboard tilt="play" dart="T20" />);
  expect(container.querySelector("[data-dart-marker]")).not.toBeNull();
});
```
- [ ] **Step 2 — run, expect FAIL** (`npm --prefix ui test -- src/components/Dartboard.test.tsx`).
- [ ] **Step 3 — implement.** Add `tilt?: "flat"|"play"|"hero"` (default `"flat"`) and `dart?: string` to `Props`. Wrap the `<svg>` in a `<div className="dartboard-3d tilt-<tilt>">` ONLY when `tilt !== "flat"` (flat returns the bare svg as today, so History/heatmap is byte-for-byte unchanged). When `dart` is set, compute its bed center (reuse `segAngles`/`polarToXY`, and the bull radii) and render a `<g data-dart-marker>` (ring + dot) at that point.
- [ ] **Step 4 — run, expect PASS**; confirm existing Dartboard + History tests still pass.
- [ ] **Step 5 — commit** `feat(ui): Dartboard tilt + dart-marker props (flat default preserved)`.

### Task 1.2: Dartboard stylish 3D base (visual)

**Files:** Modify `ui/src/components/Dartboard.tsx`, `ui/src/index.css`; Test `ui/src/components/Dartboard.test.tsx`.

- [ ] **Step 1 — failing test** (structure assertions for the new visual scaffolding):
```tsx
it("includes the metallic wire gradient + contact shadow when tilted", () => {
  const { container } = render(<Dartboard tilt="play" />);
  expect(container.querySelector("#gb-metal")).not.toBeNull();        // wire gradient def
  expect(container.querySelector("[data-contact-shadow]")).not.toBeNull();
});
```
- [ ] **Step 2 — run, expect FAIL.**
- [ ] **Step 3 — implement.** When tilted: add `<defs>` with a `linearGradient id="gb-metal"` (e8edf2→9aa3ab→5b636b) used for segment strokes + a raised rim ring; a sheen radial overlay; a contact-shadow `<ellipse data-contact-shadow>`; a thin board-thickness disc behind. In `index.css` add `.dartboard-3d{perspective:1000px}` and `.tilt-play svg{transform:rotateX(17deg);transform-origin:center 58%} .tilt-hero svg{transform:rotateX(26deg) rotateZ(-3deg)}` + a `.dartboard-3d svg{transition:transform .4s ease}`. Respect reduced-motion (no transform when reduced). Flat mode keeps the current thin dark wire.
- [ ] **Step 4 — run, expect PASS** (+ existing green).
- [ ] **Step 5 — commit** `feat(ui): stylish 3D dartboard treatment (metallic wire, rim, depth, shadow)`.

### Task 1.3: LiveGame uses play tilt + last-dart marker

**Files:** Modify `ui/src/views/LiveGame.tsx`; Test `ui/src/views/LiveGame.test.tsx` (create if absent).

- [ ] **Step 1 — failing test.** Render `<LiveGame>` with a state + a store `lastHit` of bed `T20`; assert the Dartboard wrapper has `tilt-play` and a `[data-dart-marker]` is present.
- [ ] **Step 2 — run, expect FAIL.**
- [ ] **Step 3 — implement.** Pass `tilt="play"` and `dart={lastHit?.bed}` to `<Dartboard>`.
- [ ] **Step 4 — run PASS.**
- [ ] **Step 5 — commit** `feat(ui): live board mounted at play tilt with dart-landing marker`.

### Task 1.4: Scoreboard dynamism (X01 first, shared patterns)

**Files:** Modify `ui/src/components/boards/X01Board.tsx` (+ apply active-player + change-pop to the other 5 boards); `ui/src/index.css`; Test `ui/src/components/boards/X01Board.test.tsx`.

- [ ] **Step 1 — failing test.** Assert the active player's card carries an emphasis class (e.g. `data-active`), and that the score element uses `tabular-nums` + a `score-pop`-capable class (`data-score`). Add a test that a re-render with a changed score keeps the score element present (animation is CSS, so test the hook/class, not the animation).
- [ ] **Step 2 — run, expect FAIL.**
- [ ] **Step 3 — implement.** Add `data-active`/emphasis ring+glow to the active card; wrap the score number in a span keyed by value so React remounts it → CSS `score-pop` plays on change; add a `bust-shake` class binding when `state.mode_view` signals a bust (if available; otherwise leave a TODO-free no-op hook). Mirror the active-card emphasis in Cricket/Atc/FreePlay/CountUp/Medley boards.
- [ ] **Step 4 — run PASS** (all board tests green).
- [ ] **Step 5 — commit** `feat(ui): livelier scoreboards (active emphasis, score-change pop)`.

**Green-gate Slice 1:** `npm --prefix ui test` + `npm --prefix ui run build` all green.

---

## Slice 2 — Big moments (checkout & win)

### Task 2.1: Board swings to hero tilt on win/checkout

**Files:** Modify `ui/src/App.tsx` (or `LiveGame`); Test in `App.test.tsx` or a small dedicated test.

- [ ] **Step 1 — failing test.** Drive a `game_won`/`leg_won` banner (as `App.test` already constructs banners) and assert the live Dartboard wrapper transiently gets `tilt-hero` (e.g. via a store/state flag `boardHero`).
- [ ] **Step 2 — run FAIL.**
- [ ] **Step 3 — implement.** Add a transient `boardHero` flag (store or local state) set true when the latest banner is `leg_won`/`game_won`, auto-clearing after ~2.5s; pass `tilt={boardHero ? "hero" : "play"}` to the live Dartboard. Reduced-motion → stay at play tilt.
- [ ] **Step 4 — run PASS.**
- [ ] **Step 5 — commit** `feat(ui): dartboard swings to cinematic angle on checkout/win`.

### Task 2.2: Sound cues on the moments

**Files:** Verify `ui/src/sound/decide.ts` maps `one-eighty`/`checkout-available`/`leg-won`/`game-won`; wire any missing trigger; Test `ui/src/sound/decide.test.ts`.

- [ ] **Step 1 — failing test** (only if a mapping is missing): assert `decide` returns the cue for the event.
- [ ] **Step 2–4 — implement/verify/PASS.** (If all cues already fire, note it and skip to commit — `SynthPack` already defines them.)
- [ ] **Step 5 — commit** `feat(ui): ensure celebration sound cues fire on big moments` (skip if no change).

### Task 2.3: Tighten celebration sequence + reduced-motion

**Files:** Modify `ui/src/components/CheckoutOverlay.tsx` / `Celebration.tsx`; Tests already exist.

- [ ] **Step 1 — failing test** for any new behavior (e.g. the overlay also shows the leg/match context label). Keep existing reduced-motion + fallback tests green.
- [ ] **Step 2–4.** Refine timing/visuals; verify all overlay tests pass incl. reduced-motion short-circuit.
- [ ] **Step 5 — commit** `feat(ui): tighten checkout/win celebration sequence`.

**Green-gate Slice 2.**

---

## Slice 3 — Screen polish & cohesion

### Task 3.1: Shared page primitives

**Files:** Create `ui/src/components/Page.tsx` (exports `PageHeader`, `Section`, `EmptyState`); Test `ui/src/components/Page.test.tsx`.

- [ ] **Step 1 — failing test.** `PageHeader` renders its title (role heading); `EmptyState` renders its message + optional icon; `Section` renders its heading + children.
- [ ] **Step 2 — FAIL.**
- [ ] **Step 3 — implement** small presentational components using the theme tokens (amber heading, surface cards, consistent spacing).
- [ ] **Step 4 — PASS.**
- [ ] **Step 5 — commit** `feat(ui): shared Page/Section/EmptyState primitives`.

### Task 3.2: Apply cohesion to the four screens

**Files:** Modify `Setup.tsx`, `Profile.tsx`, `Leaderboard.tsx`, `History.tsx`; keep their existing tests green (adjust selectors only if necessary, never weaken assertions).

- [ ] **Step 1 — verify tests** for each screen still pass after refactoring to use the primitives (the primitives must preserve existing text/roles the tests assert).
- [ ] **Step 2–4.** Replace ad-hoc headers/empty-states with the primitives; add a subtle `fade-in` on tab switch via an `index.css` keyframe applied at the content root in `App.tsx`.
- [ ] **Step 5 — commit** `feat(ui): cohesive headers/empty-states/transitions across screens`.

**Green-gate Slice 3.**

---

## Slice 4 — Multiplayer broadcast layout + Fullscreen

### Task 4.1: `useFullscreen` hook

**Files:** Create `ui/src/useFullscreen.ts`; Test `ui/src/useFullscreen.test.ts`.

- [ ] **Step 1 — failing test.**
```ts
import { renderHook, act } from "@testing-library/react";
import { useFullscreen } from "./useFullscreen";
it("reports not-fullscreen and exposes toggle in a plain DOM env", () => {
  const { result } = renderHook(() => useFullscreen());
  expect(result.current.isFullscreen).toBe(false);
  expect(typeof result.current.toggle).toBe("function");
});
it("toggle does not throw when no fullscreen API is present", async () => {
  const { result } = renderHook(() => useFullscreen());
  await act(async () => { await result.current.toggle(); });
});
```
- [ ] **Step 2 — FAIL.**
- [ ] **Step 3 — implement.** Detect Tauri (`"__TAURI_INTERNALS__" in window` or try `import("@tauri-apps/api/window")`); if present use `getCurrentWindow().setFullscreen(!current)`; else use the browser `document.documentElement.requestFullscreen()` / `document.exitFullscreen()`, all wrapped in try/catch and guarded for jsdom (no `requestFullscreen` → no-op). Track `isFullscreen` via state + `fullscreenchange` listener (browser) or the Tauri value.
- [ ] **Step 4 — PASS.**
- [ ] **Step 5 — commit** `feat(ui): useFullscreen hook (Tauri window + browser fallback)`.

### Task 4.2: `FullscreenToggle` in header

**Files:** Create `ui/src/components/FullscreenToggle.tsx`; Modify `ui/src/App.tsx`; Test `ui/src/components/FullscreenToggle.test.tsx`.

- [ ] **Step 1 — failing test.** Renders a button with an accessible label ("Toggle fullscreen"); clicking calls the hook's `toggle` (mock the hook).
- [ ] **Step 2 — FAIL.**
- [ ] **Step 3 — implement** the button (enter/exit icon via inline SVG), mount it in the header next to `SoundToggle`/`VideoToggle`.
- [ ] **Step 4 — PASS.**
- [ ] **Step 5 — commit** `feat(ui): fullscreen toggle in header`.

### Task 4.3: `MpGameLayout` broadcast-rail shell

**Files:** Create `ui/src/components/MpGameLayout.tsx`; Test `ui/src/components/MpGameLayout.test.tsx`.

- [ ] **Step 1 — failing test.** Given props `{ board, selfVideo, oppVideo, oppCard, controls }` (ReactNodes), it renders all five regions and the root carries `data-mp-layout` with a viewport-height class. Assert the board region and the opponent video region are both present (no-scroll structure).
- [ ] **Step 2 — FAIL.**
- [ ] **Step 3 — implement** a flex layout: `h-[calc(100vh-…)]`/`min-h-0` main row → left board-zone (`flex-1`, centers board+score) + right rail (`w-[34%]`, column: oppVideo `flex-[1.4]`, selfVideo `flex-1`, oppCard, controls `mt-auto`). `overflow-hidden` so it never scrolls; internal regions get `min-h-0`.
- [ ] **Step 4 — PASS.**
- [ ] **Step 5 — commit** `feat(ui): MpGameLayout broadcast-rail shell (no-scroll)`.

### Task 4.4: Multiplayer view uses the layout in-game

**Files:** Modify `ui/src/views/Multiplayer.tsx`; Test `ui/src/views/Multiplayer.test.tsx`.

- [ ] **Step 1 — failing test.** With `mpStatus="in_room"` + a `gameState` in_progress + a peer, assert the rendered tree contains `[data-mp-layout]` and both video tiles + the board. Pre-game (no in-progress game) keeps the existing lobby (assert the join/start UI still shows).
- [ ] **Step 2 — FAIL.**
- [ ] **Step 3 — implement.** When `gameState?.status === "in_progress"`, render `<MpGameLayout board={<LiveGame…/>} selfVideo={…} oppVideo={…} oppCard={opponentCard && <OpponentCard…/>} controls={<MpControls…/> + guest/host controls} />`. Otherwise keep the current lobby/video-grid stack. Keep the room header minimal/out of the layout or as a slim bar.
- [ ] **Step 4 — PASS** (all Multiplayer tests green).
- [ ] **Step 5 — commit** `feat(ui): multiplayer in-game uses no-scroll broadcast layout`.

**Green-gate Slice 4** + full suite: `npm --prefix ui test` + `npm --prefix ui run build` + `python -m pytest -q` + `python -m pytest server -q`.

---

## Integration & release

- Open a PR from `feat/ui-polish-v0.1.4` → `main` once all slices are green (or stack incrementally). Never local-merge while the sibling holds the shared checkout.
- After merge, cut **v0.1.4** per `docs/RELEASING.md`: bump `tauri.conf.json` + `pyproject.toml` to `0.1.4`, full signed build (needs the user's minisign key password), `latest.json`, all six assets, verify updater chain returns `0.1.4`. Also re-test bug #1 (local manual scoring) on this build and confirm bug #3 (History) in the package.
