interface:
  display_name: "Granbridge"
  short_description: "Repo-specific patterns and workflows for granbridge"
  default_prompt: "Use the granbridge repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true